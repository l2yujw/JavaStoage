    @ ctl+d (MAC Command + d)
        - 아래 복사

    @ = 뒤의 코드 작성 후 ctrl+alt+v 누르면 그에 맞는 변수 타입을 생성해줌 (MAC Option+Command+V)
        ex) "hello".equlas("hello"); => boolean hello ~~

    @ ctrl+shift+enter (MAC shift+command+enter)
        - 코드 끝에 세미콜론을 넣어줌

    @ ctrl+shift+f10 (MAC ctrl+shift+r)
        - Run Configure 생성 후 컴파일

    @ ctrl+x => ctrl+v 잘라서 붙여넣기 (MAC Command+x)

    @ ctrl+e => 클래스들을 보여줌 엔터 누르면 이전 클래스로 감

    @ ctrl+b (MAC command b)
        - 해당 메서드로 이동함

    @ iter
        - for-each 자동완성

    @ ctrl + alt + v (MAC alt+command+v)
        -> 뒷 내용에 대한 데이터타입과 변수명을 나타내줌

    @ ctrl + p (MAC command + p)
        - 빨간줄 뜰때 문제점을 보여줌

    @ alt + insert (command n)
        - Generate

    @ ctrl + o
        - 오버라이딩 보여줌